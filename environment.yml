name: hsp2_py38
channels:
  - defaults
  # - conda-forge
  # - https://conda.anaconda.org/conda-forge
dependencies:
  # Running HSP2
  - python>=3.8.0
  - numba
  - numpy
  - pandas>=1.0.0
  - pytables
  - networkx
  # Interacting with HSP2
  - jupyterlab
  - matplotlib
  # package management
  - conda
  - conda-build
  - pip
  # optional
  # - nodejs  # required for `jupyter-matplotlib` & `jupyterlab/toc` extensions
